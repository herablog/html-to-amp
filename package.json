{
  "name": "html-to-amp",
  "version": "0.0.11",
  "description": "Transform the normal html to the amp html.",
  "main": "index.js",
  "engines": {
    "node": ">=4"
  },
  "scripts": {
    "test": "npm run eslint && npm run mocha",
    "eslint": "eslint src/",
    "mocha": "mocha --compilers js:babel-register --recursive tests/",
    "build": "babel src --out-dir lib"
  },
  "author": "herablog",
  "license": "MIT",
  "dependencies": {
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.4.3",
    "cheerio": "^0.19.0"
  },
  "devDependencies": {
    "babel-cli": "^6.4.5",
    "chai": "^3.4.1",
    "eslint": "^1.10.3",
    "eslint-config-airbnb": "^3.1.0",
    "mocha": "^2.3.4"
  },
  "private": true
}
